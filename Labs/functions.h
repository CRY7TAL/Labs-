#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#define K 3
#define N 4

float calculateSpeed(float L, float v1, float t);
double summa(double x, int n);
int countWords(int length);
void processLine(char buffer[]);
void selectiveZero(int x[], int size);
void Sort(int arr[], int size);
void printBinary(unsigned long num);

#endif
